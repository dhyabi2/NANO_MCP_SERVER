/** @hidden */
export declare function checkString(candidate: any): boolean;
/** @hidden */
export declare function checkNumber(candidate: any): boolean;
/**
 * Check if the given balance is valid.
 * Does not require initialization.
 *
 * @param balance - The balance to check
 * @returns Valid
 */
export declare function checkBalance(balance: any): boolean;
/**
 * Check if the given seed is valid.
 * Does not require initialization.
 *
 * @param seed - The seed to check
 * @returns Valid
 */
export declare function checkSeed(seed: any): boolean;
/**
 * Check if the given hash is valid.
 * Does not require initialization.
 *
 * @param hash - The hash to check
 * @returns Valid
 */
export declare function checkHash(hash: any): boolean;
/**
 * Check if the given public or secret key is valid.
 * Does not require initialization.
 *
 * @param key - The key to check
 * @returns Valid
 */
export declare function checkKey(key: any): boolean;
/**
 * Check if the given address is valid.
 * Does not require initialization.
 *
 * @param address - The address to check
 * @returns Valid
 */
export declare function checkAddress(address: any): boolean;
/**
 * Check if the given work is valid.
 * Does not require initialization.
 *
 * @param work - The work to check
 * @returns Valid
 */
export declare function checkWork(work: any): boolean;
/**
 * Check if the given signature is valid.
 * Does not require initialization.
 *
 * @param signature - The signature to check
 * @returns Valid
 */
export declare function checkSignature(signature: any): boolean;
